import { votingAddress, votingAbi } from "@/components/VotingContractUtils"
import Bar from '@/components/voting/workflowstatus/Bar'

const WorkFlowStatus = ( { isOwner } ) => {
  }

export default WorkFlowStatus
